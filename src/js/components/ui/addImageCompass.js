export default function (container, angle)
{

    /*
    var leftCompass = $('<div/>').css({
       position: 'absolute',
       bottom: '5px',
       left: '10px',
       color: '#fff',
       'font-weight': 'bold',
       'z-index': '5000'
    });

    var rightCompass = $('<div/>').css({
        position: 'absolute',
        bottom: '5px',
        right: '10px',
        color: '#fff',
        'font-weight': 'bold',
        'z-index': '5000'
    });

    var leftAngle = 'none', rightAngle = 'none';

    let direction = ['N', 'NNE', 'NE', 'ENE', 'E', 'ESE', 'SE', 'SSE', 'S', 'SSW', 'SW', 'WSW', 'W', 'WNW', 'NW', 'NNW', 'N'][Math.round(angle / 11.25 / 2)];

    if (direction == 'N') {
        leftAngle = "W";
        rightAngle = "E";
    } else if (direction == 'NNE' || direction == "NE" || direction == "ENE") {
        leftAngle = "NW";
        rightAngle = "SE";
    } else if (direction == 'E') {
        leftAngle = "N";
        rightAngle = "S";
    } else if (direction == "ESE" || direction == "SE" || direction == "SSE") {
        leftAngle = "NE";
        rightAngle = "SW";
    } else if (direction == "S") {
        leftAngle = "E";
        rightAngle = "W";
    } else if (direction == "SSW" || direction == "SW" || direction == "WSW") {
        leftAngle = "SE";
        rightAngle = "NW";
    } else if (direction == "W") {
        leftAngle = "S";
        rightAngle = "N";
    } else if (direction == "WNW" || direction == "NW" || direction == "NNW") {
        leftAngle = "SW";
        rightAngle = "NE";
    }

    leftCompass.append(leftAngle);
    rightCompass.append(rightAngle);

    container.append(leftCompass);
    container.append(rightCompass);*/

    return;
}